language: c

os:
  - osx

jobs:
  include:
    - os: osx
      osx_image: xcode11
    - os: osx
      osx_image: xcode8

script: |
  pip install --upgrade --requirement tests/requirements.txt
  pytest --capture=no --verbose tests
